apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod-busybox
  name: pod-busybox
spec:
  containers:
  - image: busybox
    name: pod-busybox
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
