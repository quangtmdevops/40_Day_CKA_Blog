apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod5
  name: pod5
spec:
  containers:
  - image: redis
    name: pod5
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
